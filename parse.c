#include <stdio.h>
#include "scanner.h"


//Return how big an object file output could be
int parse_outSize(FILE *infile)
{
	return 0;
}

//Print label and address insert data
void parse_labelData(FILE *infile, FILE *outfile)
{
	return;
}

//Print assembly data
void parse_assemble(FILE *infile, FILE *outfile)
{
	return;
}

